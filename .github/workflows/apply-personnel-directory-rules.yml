name: Apply personnel directory access rules

on:
  push:
    branches:
      - production-readiness-cleanup

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Smoke test
        run: echo "personnel directory patch workflow started"
